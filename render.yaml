services:
  - type: web
    name: mini-google-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn search_api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
